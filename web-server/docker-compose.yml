version: '3.8'

services:
  api:
    build: ./server
    ports:
      - "8438:8438" 
    environment:
      MONGODB_URI: mongodb://mongo:27017/mydatabase  
    depends_on:
      - mongo
  mongo:
    image: mongo:latest
    volumes:
      - mongo-data:/data/db 

volumes:
  mongo-data: